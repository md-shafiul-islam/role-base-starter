import React from "react";
import UserAddForm from "@/src/Components/User/UserAddForm";

const userAddIndexPage = (props) => {
  return (
    <React.Fragment>
      <UserAddForm />
    </React.Fragment>
  );
};

export default userAddIndexPage;
