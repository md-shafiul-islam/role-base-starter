import React from "react";
import RoleTable from "@/src/Components/Role/RoleTable";

const roleIndexPage = (props) => {
  return (
    <React.Fragment>
      <RoleTable />
    </React.Fragment>
  );
};

export default roleIndexPage;
